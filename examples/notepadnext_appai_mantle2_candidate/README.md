# NotepadNext.AppAI Mantle 2 candidate

This is a distinct maintained candidate. It does not overwrite or reinterpret the
historical resident, lifecycle spore, terminal, or audits under
`work/notepadnext-assimilation/`.

The candidate is pinned to NotepadNext commit
`0e9694d98aa8a9962bbe2bfa9dd502931be33670`. Its terminal delegates the canonical
command and conversation paths to `ResidentRuntime`; `/surfaces` is a host-specific
Body extension. The lifecycle output uses germ v2, embeds and hashes the terminal
source, requires target-bound activation authorization, and records a v2 completion
audit. It never claims that the observed Qt graph is an applied or verified hook.

Build disposable evidence outside this source directory:

```text
python examples/notepadnext_appai_mantle2_candidate/build_candidate.py --out work/notepadnext-mantle2-candidate
python examples/notepadnext_appai_mantle2_candidate/terminal.py work/notepadnext-mantle2-candidate/resident-a
```

The builder hatches the same spore twice and proves the independently minted Body
keys differ. No NotepadNext source or binary is modified.
