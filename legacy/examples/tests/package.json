{
  "name": "mantle-demo-smoke",
  "version": "1.0.0",
  "private": true,
  "description": "Headless smoke tests for the Mantle OS reference HTML demos (the Python substrate is certified separately by `python -m mantle prove`).",
  "type": "module",
  "scripts": {
    "test": "node demo_smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
