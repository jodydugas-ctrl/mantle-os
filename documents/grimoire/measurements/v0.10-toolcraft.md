# Grimoire v0.10 toolcraft measurement

This report is a synchronized projection of
[`v0.10-toolcraft.json`](v0.10-toolcraft.json). The canonical source is the JSON file.

## Result

The approved v0.10 edition declares a target of nine toolcraft statements, but the
approved source contains no labeled toolcraft section, no B11/B12 clauses, and no separate
toolcraft vectors. Therefore the direct corpus measurement is **unmeasured**: zero of the
declared nine statements were available for blind decoding. No concept, relation, or
composition claim is promoted from absence.

| Measure | Status | Reason |
| --- | --- | --- |
| Nine-statement concept recovery | unmeasured | No nine-statement toolcraft corpus is present in the source. |
| Nine-statement relation recovery | unmeasured | No vectors or intended source interpretations are present. |
| `tool` composition boundary | unmeasured | The source supplies no independently inspectable boundary. |
| `handwork` composition boundary | unmeasured | The source supplies no independently inspectable boundary. |

The source and decoder hashes, target count, observed count, and unknowns are recorded in
the JSON artifact. A future operator-approved source supplement may replace this status;
this report does not infer the missing material and does not grant adoption authority.
